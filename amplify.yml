version: 1
frontend:
  phases:
    preBuild:
      commands: ['npm install']  # Install dependencies
    build:
      commands: ['npm run build']  # Build command
  artifacts:
    baseDirectory: /dist  # Replace with your build output directory (e.g., dist, build)
    files:
      - '**/*'  # Include all files in the artifact
